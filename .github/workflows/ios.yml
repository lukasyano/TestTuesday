name: Swift Build

on:
  push:
      branches:
      - 'release/*'
      - 'develop'
      - 'master'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Swift
      uses: swift-actions/setup-swift@v1
    - name: Build
      run: swift build

    - name: Test
      run: swift test
